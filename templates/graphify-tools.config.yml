# graphify-tools.config.yml
# Configuration for graphify-mcp-tools

# Output directory (relative to config file location)
output: graphify-out

# Repositories to include in multi-repo build
repos:
  - name: my-project
    path: .

# Build options
build:
  graphify_args: []         # extra arguments passed to graphify CLI (merge-graphs, update)

# Outline options
outlines:
  enabled: true
  language: python
  paths:
    - "src/**/*.py"
  exclude:
    - "**/__pycache__/**"
    - "**/test_*.py"
    - "**/.git/**"

# Search index options
search:
  enabled: true
  fields:
    - label
    - type
    - source_file
    - properties

# MCP server options
server:
  # Reserved for future configuration
